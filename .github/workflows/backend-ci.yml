name: Backend CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Restore
        run: dotnet restore OneBankToRuleThemAllAPI.sln

      - name: Build
        run: dotnet build OneBankToRuleThemAllAPI.sln --configuration Release --no-restore

      - name: Test
        run: dotnet test OneBankToRuleThemAllAPI.sln --configuration Release --no-build
